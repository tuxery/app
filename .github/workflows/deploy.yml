name: Deploy — Cloudflare Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    name: Build & deploy apps/web
    runs-on: ubuntu-latest
    # No-ops until CLOUDFLARE_API_TOKEN / CLOUDFLARE_ACCOUNT_ID repo secrets
    # are configured — see apps/web/README.md.
    if: ${{ vars.CLOUDFLARE_ACCOUNT_ID != '' || secrets.CLOUDFLARE_API_TOKEN != '' }}
    steps:
      - uses: actions/checkout@v7

      - uses: helpers4/action/setup-pnpm@6fddd178620e7325d446a246c38a44f979569fc4 # main @ 2026-07-29
        with:
          node-version: "24"

      - name: Build (Cloudflare Pages adapter)
        working-directory: apps/web
        run: pnpm run build

      - name: Deploy to Cloudflare Pages
        working-directory: apps/web
        run: pnpm dlx wrangler pages deploy dist --project-name=tuxery
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
